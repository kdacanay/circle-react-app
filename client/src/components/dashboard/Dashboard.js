// fetch all data using an action and bring in redux state and    then pass to other components

import React from 'react';
import PropTypes from 'prop-types';

const Dashboard = props => {
  return (
    <div>
      Dashboard
    </div>
  );
};

Dashboard.propTypes = {

};

export default Dashboard;
